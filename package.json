{
    "license": "BSD-3-Clause",
    "scripts": {
        "build": "esbuild --bundle concurrent-task.js --outdir=build",
        "start": "node dev-server.js"
    },
    "devDependencies": {
        "elm": "^0.19.1-6",
        "elm-review": "^2.13.4"
    },
    "type": "module",
    "engines": {
        "node": ">=23.0.0"
    }
}
